# LearnLove 全链路重构计划

更新时间：2026-09-01

## 目标

把微信原始消息稳定转换成统一、可追踪的聊天事件；完整归档文字、引用、语音、图片及用户主动讲述的事件/故事；让后续上下文、总结和复盘基于同一份事实数据，减少错序、错人、漏信息与媒体丢失。

## 当前阶段

状态：进行中 — 统一归档和主查询解析已落地，正在统一后台监听、补齐解析验收，并优化 Agent 时间线输出。

## 工作项

- [x] 盘点现有数据流与已知混乱来源
- [x] 建立统一消息、事件、媒体归档模型
- [x] 重构消息排序、去重、发送者识别、引用与类型解析
- [ ] 新增事件/故事总结、查看、检索和上下文注入
- [x] 建立本地/API 可插拔的语音与识图适配器、待处理队列和结果归档
- [x] 接入 LearnLove 专用 SenseVoice 与 Qwen2.5-VL 模型、执行器和视觉投影
- [x] 新增面向 Agent 的规范时间线输出，分离事实、引用、媒体识别和诊断字段
- [ ] 补齐单元测试、跨项目集成测试与文档

## 设计原则

- 原始事实与模型总结分层保存；总结可以重做，原始消息不能丢。
- 所有记录使用稳定 ID、原始时间戳和明确来源，禁止只靠时间戳去重。
- 媒体文件先归档再识别；API 未配置或失败时保留待处理状态，可稍后补跑。
- 事件/故事既可由用户主动记录，也可从一段对话中总结；必须保留证据消息 ID 与时间范围。
- 新结构兼容现有记忆与复盘文件，不破坏用户已有数据。

## 已确认问题

- 旧消息使用“时间 + 发送者 + 内容片段”去重，同秒消息容易碰撞，也可能漏掉游标边界上的消息。
- 数据库已有 `server_id`、`sort_seq`、`server_seq`、`packed_info_data`，旧链路没有利用这些稳定标识与排序字段。
- `Name2Id` 可提供发送者映射，但旧链路主要依赖消息 XML 猜测，导致发送者标记不稳定。
- 引用、合并转发、位置、名片、系统消息和附件元数据没有统一结构，最终展示容易混成正文。
- 本地 Whisper 与消息读取耦合，没有“先归档、后识别”的媒体队列，API 缺失时也无法稳定补跑。

## 已确定结构

- `archive/records.db`：统一保存规范化消息、事件/故事、事件修订、媒体记录与 API 任务。
- `archive/media/`：保存语音原始数据、图片原始加密文件及可解码版本，识别失败也不丢源文件。
- `agent/wechat_parser.py`：只负责微信消息规范化，不承担存储和模型调用。
- `agent/archive.py`：负责幂等归档、事件版本与上下文读取。
- `agent/media.py` 与 `agent/media_api.py`：负责文件落档、可插拔本地/API 适配和可重试队列；原始结果与处理器信息都要归档。

## 2026-08-31：本地媒体模型与 Agent 时间线

- [x] 发现语音模型 `D:\Llama\models\sense-voice-small-q4_0.gguf`
- [x] 发现视觉模型 `D:\Llama\models\Qwen2.5-VL-3B-Instruct-Q4_K_M.gguf`
- [x] 确认 SenseVoice 运行条件：SILK 解码器已存在，当前缺少 `llama-funasr-sensevoice.exe` 专用执行器
- [x] 确认 Qwen2.5-VL 运行条件：`llama-mtmd-cli.exe` 已存在，当前缺少匹配的 `mmproj` 视觉投影文件
- [x] 统一手动历史、增量检查和后台监听的规范化解析路径
- [x] 增加 `learnlove.chat.timeline.v1` Agent 时间线契约和解析诊断
- [x] 用合成样本与真实库匿名统计完成解析验收

解析验收结果：真实库匿名抽样 2300 条、跨 5 个消息库，稳定 ID 重复 0、时间顺序正确、发送者归类 100%、未知消息类型 0；147 条引用中 146 条含文本，另 1 条为无文本媒体引用并保留类型/证据结构。发现并修复实际库中 87 条应用消息因非法 XML 控制字符导致的整体解析失败，修复后应用子类型全部恢复。新增解析、时间线、本地媒体条件及 TJU 回归共 14 项通过，全项目 Python 语法检查通过。

## 验证记录

媒体模型阶段：15 项 unittest 全部通过；真实微信 SILK 语音已分别通过独立命令与统一任务队列转写，统一队列结果使用 `local:sensevoice-small-q8-official` 归档；Qwen2.5-VL 已识别包含 `TEST 42` 的实际 PNG，退出码 0。两个默认能力状态均为 ready。

## 2026-09-01：LearnLove 专用媒体模型

- [x] 核对两个现有 GGUF 的大小、SHA-256、上游来源与许可证
- [x] 将语音和视觉模型迁移到 `models/media/`，不再依赖 `D:\Llama`
- [x] 将模型、视觉投影和原生运行时加入 Git 忽略
- [x] 下载 SenseVoice Windows 运行时和 Qwen2.5-VL 对应 mmproj
- [x] 建立可扩展模型注册表，支持替换默认模型和增加同能力模型
- [x] 接入本地语音转写与图片理解处理器，并完成真实冒烟测试
- [x] 补齐来源、安装、校验和扩展说明

本次边界：仓库只提交注册表、代码、来源、许可证说明和安装脚本；GGUF、mmproj、DLL、EXE 等大文件只保存在本机 LearnLove 目录。迁移前后均用文件大小和 SHA-256 校验，运行器必须与模型格式实际加载验证，不能只按文件名判断兼容。

实测补充：原有 q4_0 使用新版 FunASR 运行器会报告张量格式不匹配；使用匹配的 SenseVoice.cpp v1.4.0 可完整加载，但在当前 Windows CPU 推理阶段访问冲突，因此保留为已登记但默认停用的旧模型。为保证功能可用，新增并默认启用官方 SenseVoiceSmall q8；真实 6.14 秒微信语音转写成功。视觉模型与 mmproj 真实识图成功。Git `check-ignore` 已确认模型和运行时均被忽略，`git ls-files models/** runtime/**` 为空。

## 2026-09-01：按命令选择联系人、媒体开关与全局记忆

- [x] 取消启动时从固定 contacts 配置自动选中联系人和自动监听
- [x] 让 `/contact <名称>` 成为唯一的联系人指向入口
- [x] 增加全局 `/voice`、`/image` 媒体模型开关及状态命令
- [x] 新增面向用户本人的全局记忆，注入所有咨询和联系人上下文
- [x] 增加配置说明、文档和回归测试

## 2026-09-01：复盘按联系人归档与旧媒体清理

- [x] 新复盘报告写入 `reviews/<联系人>/`
- [x] 将根目录既有 25 份报告按联系人迁移，并保留新旧目录兼容读取
- [x] 清理旧 q4_0 SenseVoice 模型、旧执行器、源码与构建前置
- [x] 完成回归检查与本地提交 `107e69f`；远端推送待用户再次明确确认

## 2026-09-01：关系判断校准

- [x] 在 `/help` 增加事件/故事的直接归档与查询入口
- [x] 将媒体命令改为明确的开启、关闭、当前状态与手动补跑说明
- [x] 为咨询和复盘增加信号、反证、趋势、真诚沟通与止损的强制判断纪律
- [x] 补齐监听链路的自动媒体处理与结果回填；失败任务仅在手动补跑时重试
- [x] 新增五维关系信号归档、趋势仪表盘、预警与上下文注入；不生成单一好感总分
- [x] 新增原始消息长期统计：相邻时段的数量、长短句、媒体类型和关系短语，并自动提供近期趋势上下文
- [x] 支持按联系人的历史消息回填（不批量复制媒体），统计未给窗口时按真实归档覆盖范围自动选择
- [x] 新增月→周→日的紧凑统计下钻工具，避免大规模聊天原文和 XML 卡片挤爆上下文
- [x] 新增按联系人分目录的阶段分析底稿，保存事实、解释、不确定项和核对点
- [x] 阶段底稿升级为稳定 ID 和版本快照，支持读取、复用和带原因的修订
- [x] 新增并默认启用“关系趋势校准”技能，与狗头军师分工处理长期状态、阶段变化和旧记忆冲突
- [ ] 设计可复核的好感度、情绪和沟通压力跟踪，不把推断伪装成事实

## 2026-08-30：DeepSeek 上下文保真修复

- [x] 盘点模型窗口、会话历史与长期记忆压缩链路
- [x] 将官网 DeepSeek V4 档案设为 1M 上下文，并让运行时实际采用模型档案预算
- [x] 移除会话和 transcript 的静默切片，改为原文持久化、超预算时分批摘要
- [x] 保证记忆压缩逐批覆盖全部待处理记录，任一批失败时不清空原记录
- [x] 增加上下文预算、分批覆盖和失败保留测试
- [ ] 执行 Python 自动化回归（当前 `.venv` 指向已删除的 Python 3.14.6，系统无其他 Python）

本次原则：模型窗口是输入预算，不等同于每轮都塞满 1M；正常情况下保留完整近期会话，接近窗口才压缩。压缩只替换模型工作上下文，原始 transcript 与归档不静默裁剪，确保重要细节仍可追溯。

静态验证：`git diff --check` 通过；已确认 4 个官网 DeepSeek 档案均为 1M、会话尾部切片和 transcript 头尾裁剪代码均已移除。自动化测试待 Python 环境恢复后运行 `python -m pytest -q`。

## 2026-08-30：Hub 归档恢复与 LearnLove 对话账本

- [x] 确认 Hub“完全归档”会删除实例与活动 Session，只保留只读事件归档
- [x] 建立 LearnLove 独立的追加式对话账本，自动保存用户、助手、微信监听及工具事件
- [x] 提供分页读取、关键词搜索和按会话恢复工作上下文能力
- [x] 支持导入 Hub 归档事件并去重，恢复后可在新会话中延续语义上下文
- [x] 增加 Agent 工具、`/restore`、`/history`、`/sessions` 命令及使用说明
- [x] 增加完整原文读取、Hub 流式重建、幂等导入和上下文恢复测试
- [ ] 在 AgentHub 增加“恢复归档”服务端入口（当前工作区无写权限，需切换到 AgentHub 工作区）

恢复语义：永久归档保持原 sid 和原始记录不变；恢复运行时使用当前可写会话，并在本地账本记录 `source_sid` 与 `hub-archive:<sid>` 会话键。模型连续性由 LearnLove 本地账本恢复，不把 Hub 展示事件直接冒充原始模型消息。

验证记录：已用现有 `8.jsonl` 检查事件契约（22 条 user、33 条 assistant_final，以及流式增量、工具、日志事件）；导入器据此优先使用完整 final，仅在没有 final 时拼接 delta。`git diff --check` 通过。Python 自动化测试仍受失效 `.venv` 阻塞。
